var ServicesModule = angular.module('starter.services', [])

ServicesModule.factory('Chats', function() {
  // Might use a resource here that returns a JSON array
  return {
    //SERVICE API RETURNED HERE
  };
});
